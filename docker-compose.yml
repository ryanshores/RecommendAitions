services:
  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://backend:8000
    ports:
      - "3000:80"

  backend:
    build: ./backend
    env_file: ./backend/.env
    ports:
      - "8000:8000"